import { user } from '@/apis/api.user';
import { common } from '@/apis/api.common';
import { join } from '@/apis/api.join';
import { store } from '@/apis/api.store';
import { product } from '@/apis/api.product';
import { company } from '@/apis/api.company';
import { permission } from '@/apis/api.permission';
import { catalog } from '@/apis/api.catalog';
import { shipping } from '@/apis/api.shipping';
import { community } from '@/apis/api.community';
import { order } from '@/apis/api.order';
import { dashboard } from '@/apis/api.dashboard';
import { commission } from '@/apis/api.commission';
import { settlement } from '@/apis/api.settlement';
import { customer } from '@/apis/api.customer';
import { map } from '@/apis/api.kakao-map';
import { coupon } from '@/apis/api.coupon';
import { revenue } from '@/apis/api.revenue';
import { master } from '@/apis/api.master';
import { ecoupon } from '@/apis/api.ecoupon';

export default {
  user,
  join,
  common,
  store,
  product,
  company,
  permission,
  catalog,
  shipping,
  community,
  order,
  dashboard,
  commission,
  settlement,
  customer,
  map,
  coupon,
  revenue,
  master,
  ecoupon,
};
